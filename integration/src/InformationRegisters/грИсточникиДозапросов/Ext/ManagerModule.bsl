#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
Процедура ОчиститьЗаписиПоИсточникуДозапросов(ID, Класс) Экспорт
	
	НаборЗаписей = РегистрыСведений.грИсточникиДозапросов.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ID.Установить(ID);
	НаборЗаписей.Отбор.Класс.Установить(Класс);
	
	НаборЗаписей.Прочитать();
	
	Если НаборЗаписей.Количество() Тогда
		НаборЗаписей.Очистить();
		НаборЗаписей.Записать();
	КонецЕсли;
	
КонецПроцедуры
	
#КонецЕсли
